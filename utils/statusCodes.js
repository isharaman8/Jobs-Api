const { StatusCodes } = require("http-status-codes");

const {
	INTERNAL_SERVER_ERROR,
	OK,
	BAD_REQUEST,
	CREATED,
	NOT_FOUND,
	UNAUTHORIZED,
} = StatusCodes;

module.exports = {
	INTERNAL_SERVER_ERROR,
	OK,
	BAD_REQUEST,
	CREATED,
	NOT_FOUND,
	UNAUTHORIZED,
};
